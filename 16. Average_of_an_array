var a = parseInt(input());
for(var i=0; i<a; i++){
	var b = input().split(" ");
	var c = b.length;
	output(Math.round((b.reduce(function(p,q){return Number(p)+Number(q)}))/(c-1)));
    output(" ");
}
