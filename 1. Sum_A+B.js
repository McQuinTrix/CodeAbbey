/*You are to add two numbers and tell their sum.
input() - will read the next line of input.
output(s) - will add the content of parameter s to the answer field on the page.*/

var a = input().split(" ");
output(parseInt(a[0])+parseInt(a[1]));
